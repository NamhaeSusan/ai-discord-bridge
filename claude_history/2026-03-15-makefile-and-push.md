# 2026-03-15 Makefile And Push

## Summary
- Added a project `Makefile` with `build`, `run`, `test`, `fmt`, `lint`, and `clean` targets.
- Documented the new make-based workflow in `README.md`.

## Verification
- `make test`
- `make lint`
